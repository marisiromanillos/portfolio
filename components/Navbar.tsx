import Link from "next/link";

const Navbar = () => {
  return (
    <>
      {/* <header className="fixed top-0 left-0 w-full px-12 bg-slate-800 z-10 py-4 mx-auto shadow-lg">
        <nav className="flex items-center justify-between max-w-7xl mx-auto">
          <div className="text-white gap-2 text-2xl font-bold flex items-center justify-between max-w-7xl mx-auto">
            <Link href="/" className="hover:text-gray-300 transition-colors">
              MySite
            </Link>
            <Link href="/" className="hover:text-gray-300 transition-colors">
              MySite
            </Link>
            <Link href="/" className="hover:text-gray-300 transition-colors">
              MySite
            </Link>
          </div>
        </nav>
      </header> */}
    </>
  );
};
export default Navbar;
